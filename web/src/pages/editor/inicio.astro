---
import EditorLayout from "../../layouts/EditorLayout.astro";
import { getSubRoutesByGroup } from "../../config/editorRoutes";

export const prerender = false;

const inicioSubroutes = getSubRoutesByGroup("inicio");
---

<EditorLayout 
  title="Panel: Inicio" 
  description="Dashboard para gestionar el contenido de la página de inicio del portal"
>
  <section class="card">
    <h2 style="margin: 0 0 16px 0;">Gestionar contenido de Inicio</h2>
    <p style="color: #666; margin-bottom: 24px;">
      Desde aquí puedes editar todos los elementos que aparecen en la página de inicio del portal DAGRAN.
    </p>

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 16px;">
      {inicioSubroutes.map((route) => (
        <a
          href={route.path}
          style="
            display: block;
            padding: 24px;
            border: 2px solid #e6e6e6;
            border-radius: 12px;
            text-decoration: none;
            background: #fff;
            transition: all 0.2s ease;
          "
          onmouseover="this.style.borderColor='#0070f3'; this.style.boxShadow='0 4px 12px rgba(0, 112, 243, 0.1)'"
          onmouseout="this.style.borderColor='#e6e6e6'; this.style.boxShadow='none'"
        >
          <div style="font-size: 3rem; margin-bottom: 12px;">
            {route.icon}
          </div>
          <h3 style="margin: 0 0 8px 0; color: #1a1a1a; font-size: 1.25rem;">
            {route.label}
          </h3>
          <p style="margin: 0; color: #666; font-size: 0.9rem;">
            {route.description}
          </p>
        </a>
      ))}
    </div>
  </section>

  <section class="card">
    <h3 style="margin: 0 0 12px 0;">Estructura de la página de Inicio</h3>
    <div style="color: #666; line-height: 1.8;">
      <ol style="margin: 0; padding-left: 20px;">
        <li><strong>Banner Superior:</strong> Imagen editable que aparece en la parte superior de todo el portal</li>
        <li><strong>Menú Principal:</strong> Navegación principal del portal (INICIO - DAGRAN - NOTICIAS - etc.)</li>
        <li><strong>Carrusel de Imágenes:</strong> Hasta 5 imágenes rotativas para contenido informativo general</li>
        <li><strong>Bloques de Información Institucional:</strong> 3 bloques destacados con información relevante</li>
      </ol>
    </div>
  </section>

  <section class="card">
    <h3 style="margin: 0 0 12px 0;">Accesos rápidos</h3>
    <div style="display: flex; gap: 10px; flex-wrap: wrap;">
      <a
        href="/"
        target="_blank"
        style="
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 10px 16px;
          border-radius: 8px;
          text-decoration: none;
          background: #0070f3;
          color: #fff;
          border: 1px solid #0070f3;
        "
      >
        <span>👁️</span>
        <span>Ver página de inicio</span>
      </a>
    </div>
  </section>
</EditorLayout>
