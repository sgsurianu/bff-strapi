# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.local
.env.production
.env.*.local

# Dependencies (ya cubiertos por subdirectorios, pero por si acaso)
node_modules/

# Build outputs (ya cubiertos por subdirectorios)
dist/
build/
.astro/

# Temporary files
*.tmp
*.temp

# Database backups (mantener local, no subir a Git)
cms/backups/db/*.db
cms/backups/*.log

# CRÍTICO: Base de datos de Strapi (NO subir, NO borrar sin backup)
cms/.tmp/data.db*
